<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rjgf.mapper.UserMapper">

<!--    登录-->
    <select id="login"   resultType="User">
        select *
        from tb_user
        where user_name = #{username}  and password= #{password}</select>

<!--    根据用户名查询用户-->
    <select id="getUserByUserName"   resultType="User">
        select *
        from tb_user
        where user_name = #{username}</select>

    <!-- 插入新用户 -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_user (user_name, password, name, age, sex, birthday)

        VALUES (#{username}, #{password}, #{name}, #{age}, #{sex}, #{birthday})
    </insert>

</mapper>
